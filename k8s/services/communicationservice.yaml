apiVersion: v1
kind: Service
metadata:
  name: communicationservice
  namespace: cartcraft
spec:
  selector:
    app: communicationservice
  ports:
    - port: 4008
      targetPort: 4008
  type: ClusterIP
